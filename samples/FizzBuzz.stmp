// prints fizzbuzz up to specified number (program arg 1)
on run:
  loop arg1 times at i:
    if (i % 3 == 0) && (i % 5 == 0):
      print "FizzBuzz"
      continue
    if i % 3 == 0:
      print "Fizz"
      continue
    if i % 5 == 0:
      print "Buzz"
      continue
    print i