package com.edavalos.stamp.Types;

// Every type of loop type statements
public enum LoopType {
    FOR,    // execute contents n times
    FORI,   // execute contents n times & keep index
    WHILE,  // execute contents while n is true
    WHILEI, // execute contents while n is true & keep index
    IF      // execute contents once if n is true
}
